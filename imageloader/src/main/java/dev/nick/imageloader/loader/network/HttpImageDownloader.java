package dev.nick.imageloader.loader.network;

import android.graphics.Bitmap;

public class HttpImageDownloader implements ImageDownloader<Bitmap> {
    @Override
    public void download(String url, DownloadListener<Bitmap> listener) {

    }
}
